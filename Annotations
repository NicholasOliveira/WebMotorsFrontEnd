# Loga no CLI Heroku
heroku login

# Vê os containers ativos
docker ps

# Loga no registry Heroku
heroku container:login

# Cria e sobe a imagem do container para o registry
heroku container:push node-server -a homolog-webmotors

# Cria uma release da imagem do container com TAG
heroku container:release node-server:1.0.0
